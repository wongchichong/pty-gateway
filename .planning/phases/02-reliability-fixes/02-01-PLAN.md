---
id: 02-01
phase: 2
wave: 1
type: reliability
autonomous: true
objective: Fix ID counter persistence bug (base36 → decimal)
requirements: [REL-01]
files_modified:
  - src/router.ts
---

# PLAN: Fix ID Counter Bug

## Objective
Change `parseInt(shortId, 36)` to `parseInt(shortId, 10)` in router.ts to fix ID collision bug after restart.

## Tasks

### Task 1: Fix ID parsing
**File**: `src/router.ts` line 75

```typescript
// BEFORE
const num = parseInt(shortId, 36);

// AFTER
const num = parseInt(shortId, 10);
```

### Task 2: Test ID generation
```bash
# Start gateway, create instances, restart, verify IDs consistent
pnpm dev
# Send: /start bash
# Restart gateway
# Send: /list
# Expected: IDs remain consistent (no collisions)
```

## Success Criteria
- [ ] ID parsing uses decimal (base 10)
- [ ] IDs consistent after restart
- [ ] No ID collisions
